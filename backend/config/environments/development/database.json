{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "bookshelf",
      "settings": {
        "client": "sqlite",
        "filename": "${process.env.DEV_DATABASE_FILENAME}"
      },
      "options": {
        "useNullAsDefault": true
      }
    }
  }
}
